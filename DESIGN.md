# Piano Scribe — 设计方案

## 1. 背景与目标

### 用户故事
- 我是钢琴爱好者，录了一段自己弹的曲子，想得到一份可打印/可编辑的谱子，方便复盘和改编。
- 我是老师，想快速把学生的弹奏录音转成谱面来批注。
- 我只有一段钢琴录音（可能是手机录的），希望软件直接产出 MIDI 和谱面。

### 目标
- 一段钢琴独奏录音 → 双谱表钢琴谱（MIDI + MusicXML + 图像），端到端可运行。
- 本地优先（隐私友好）、开源、可扩展（引擎可插拔）。

### 非目标（MVP 不做的）
- 不处理人声/其他乐器混录（需要额外的音源分离阶段，列入 V2）。
- 不做自动指法、踏板标记、表情与力度记号推断。
- 不做乐理级节奏重写（例如三连音识别、不规则划分）。

---

## 2. 总体架构

```
┌─────────────┐   ┌───────────────────────────┐   ┌──────────────────┐
│  网页前端    │──▶│  FastAPI 后端              │──▶│ 转写引擎（可插拔） │
│ (HTML/JS)   │   │  POST /api/jobs            │   │  · dsp           │
│  上传/预览   │   │  异步任务队列 + 进度轮询     │   │  · basicpitch    │
└─────────────┘   └─────────────┬─────────────┘   └────────┬─────────┘
                                │                          │ notes[]
                                ▼                          ▼
                        ┌────────────────────────────────────────┐
                        │  后处理管线                              │
                        │  BPM 估计 → 网格量化 → 重叠合并           │
                        │  pretty_midi → MIDI                     │
                        │  music21 → 双谱表 MusicXML              │
                        │  Verovio → SVG → (PNG)                 │
                        └────────────────────────────────────────┘
```

关键设计决策：**引擎与后处理解耦**。转写引擎输出统一的 `Note` 列表（MIDI、起止时间、力度），
后处理（量化、记谱、渲染）与引擎无关，因此可以随时替换/增加引擎而不影响下游。

---

## 3. 处理管线细节

### 3.1 转写
| 引擎 | 方法 | 优点 | 局限 |
|------|------|------|------|
| `dsp` | STFT 谱峰追踪 + 起音检测（librosa/scipy） | 零依赖、快、确定性 | 复杂复调易漏音/错音 |
| `basicpitch` | 卷积/循环网络逐帧预测音高与起音（Spotify） | 复调钢琴质量高 | 需 TensorFlow，较重 |

### 3.2 测速与量化
- `librosa.beat.tempo` 估计 BPM（限幅 40–208）。
- 以 16 分音符为网格对起止时间做四舍五入吸附。
- 同音高重叠音符合并（解决延音踏板/长音造成的碎片）。

### 3.3 记谱（music21）
- 以中央 C（MIDI 60）为界拆分高音/低音谱表。
- 使用 `stream.PartStaff` × 2 + `layout.StaffGroup` 生成标准双谱表（`<staves>2</staves>`）。
- 同一时刻多个音构成和弦；空白段补休止符。
- 后续可扩展：调号/拍号自动推断、按乐句分声部。

### 3.4 渲染（Verovio）
- Verovio 将 MusicXML 排版为多页 SVG（纯 Python，无需安装 MuseScore/LilyPond）。
- 可选 cairosvg 将 SVG 转 PNG（未安装时自动跳过）。

---

## 4. 技术选型及理由

| 组件 | 选择 | 理由 |
|------|------|------|
| 后端 | FastAPI + Uvicorn | 异步、自带 OpenAPI 文档、轻量 |
| 转写(ML) | Basic Pitch | 开源中最成熟的钢琴转录模型之一 |
| 转写(经典) | librosa + scipy | 零依赖回退方案，便于测试与离线部署 |
| MIDI | pretty_midi | 读写 MIDI 的标准轻量库 |
| 记谱 | music21 | 成熟的乐理对象模型，输出 MusicXML |
| 排版 | Verovio | 纯 Python 即可生成 SVG，跨平台、无外部二进制 |
| 前端 | 原生 HTML/CSS/JS | 无构建步骤，易于阅读与修改 |

---

## 5. API 设计

```
GET  /api/engines            → 可用引擎列表
POST /api/jobs               → multipart(file, engine) 创建任务，返回 JobStatus
GET  /api/jobs/{job_id}      → 查询任务状态（queued/running/done/failed）
GET  /artifacts/{job_id}/{f} → 下载产物（input.*, output.mid, output.musicxml, score.svg, score.png）
GET  /                       → Web 前端
```

`JobStatus` 字段：`job_id, status, error, engine, duration, note_count, bpm, files, notes[]`。

任务采用**线程池后台执行 + 前端轮询**的简单模型；
V2 可替换为 Celery/Redis 队列以支持多实例与持久化。

---

## 6. 数据与产物

- 输入：任意 librosa 可解码的音频（wav/mp3/flac/ogg/m4a…），≤100MB。
- 中间产物：`notes.json`（可审计、可重新生成谱面）。
- 最终产物：`output.mid`、`output.musicxml`、`score.svg`、`score.png`。

---

## 7. 质量评估

- **音高准确率**：合成已知旋律，统计检测音高与真实音高的重合率（见 `tests/test_pipeline.py`）。
- **起止误差**：检测 onset 与真实 onset 的平均绝对误差。
- 上线前建议用真实钢琴录音 + 人工标注（或与商业转谱软件对比）建立评测集。

---

## 8. 路线图

### MVP（本项目当前状态）
- [x] 端到端管线：音频 → MIDI → MusicXML → SVG
- [x] DSP 引擎开箱即用 + Basic Pitch 可选
- [x] Web 上传/进度/预览/下载 + CLI

### V1（提升可用性）
- [ ] 调号/拍号自动推断（key/beat 分析）
- [ ] 智能节奏量化（先识别音符时值聚类，再对齐网格）
- [ ] 录音预处理：去噪、压缩、均衡
- [ ] 谱面在线编辑（音符拖拽、改调、改速度）
- [ ] 多任务持久化 + 任务列表页

### V2（进阶）
- [ ] 音源分离（人声/钢琴/其他乐器分离后再转写）
- [ ] 和弦/声部分析，自动左手伴奏识别
- [ ] 指法建议、踏板标记
- [ ] 导出 PDF / 打印排版、移动端

---

## 9. 风险与备选方案

| 风险 | 应对 |
|------|------|
| Basic Pitch 依赖重（TensorFlow） | 作为可选引擎；DSP 兜底；后续可换轻量 ONNX 推理 |
| 手机录音噪声大 | 预处理降噪 + 调高起音阈值参数 |
| 复杂节奏量化不准确 | 聚类时值再量化；提供手动修正入口 |
| Verovio 排版与 MuseScore 有差异 | 同时提供 MusicXML（可在 MuseScore 打开），双轨保证 |

---

## 10. 扩展想法（长期）

- **学习模式**：转谱后逐句高亮，与录音对齐回放（跟随式练习）。
- **旋律改编**：给定和弦进行，自动为旋律配左手伴奏。
- **乐谱搜索引擎**：把「录音 → 谱面」结果与公共乐谱库做相似度匹配。
- **移动端**：手机录音直接上传，云端转谱返回 PDF。
